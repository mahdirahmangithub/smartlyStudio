/* Designer-owned prototypes. One folder per designer; each designer's
 * folder re-exports their own prototypes via that folder's index.ts. */
export type { PrototypeMeta } from "./types";
export * from "./mahdirahman";
