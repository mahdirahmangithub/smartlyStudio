/**
 * SubmenuPointerBridge — superseded.
 *
 * The physical DOM bridge has been replaced by a virtual grace polygon stored in
 * MenuGraceContext. See HoverSubmenu.tsx (onTriggerPointerLeave) for the new approach.
 */
