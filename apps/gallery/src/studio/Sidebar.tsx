// Smartly Studio — sidebar with navigation between Prototypes, Templates, Layouts, Components.
// Closed by default, opens on toggle.
// Empty for now.

export default function Sidebar() {
  return null;
}
